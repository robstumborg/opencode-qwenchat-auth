# opencode-qwenchat-auth

Fork of the Qwen OAuth plugin for OpenCode.

This is intended for personal development and testing with your own Qwen account.

## Install

In `~/.config/opencode/config.json`:

```
{
  "plugin": ["opencode-qwenchat-auth@latest"],
}
```
